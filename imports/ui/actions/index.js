export const userDropPin = (event) => ({
    type: 'USER_DROP_PIN',
    event
})

export const onSubscribed = (event) => ({
    type: 'SUBSCRIBED',
    event
})

export const userBuildingClick = (building) => ({
    type: 'USER_BUILDING_CLICK',
        building
})
